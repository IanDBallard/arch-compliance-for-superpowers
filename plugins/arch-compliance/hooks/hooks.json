{
  "description": "ACF SubagentStart constraint injection and PostToolUse Tier-1 gate (exit 2 on BLOCK findings). Requires `python` on PATH with the acf package installed; no-ops until the host has `.acf/enabled` (written by /acf-setup).",
  "hooks": {
    "SubagentStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/hooks/inject_constraints.py\" --format hook",
            "timeout": 30
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/hooks/posttooluse_gate.py\" --format hook",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
